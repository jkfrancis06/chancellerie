{% extends 'base.html.twig' %}

{% block title %}Unite index{% endblock %}

{% block body %}
    <h1>Unite index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Intitule</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for unite in unites %}
            <tr>
                <td>{{ unite.id }}</td>
                <td>{{ unite.intitule }}</td>
                <td>{{ unite.description }}</td>
                <td>
                    <a href="{{ path('unite_show', {'id': unite.id}) }}">show</a>
                    <a href="{{ path('unite_edit', {'id': unite.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('unite_new') }}">Create new</a>
{% endblock %}
