{% extends 'base.html.twig' %}

{% block title %}Corps{% endblock %}

{% block body %}
    <h1>Corps</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ corps.id }}</td>
            </tr>
            <tr>
                <th>Intitule</th>
                <td>{{ corps.intitule }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ corps.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('corps_crud_index') }}">back to list</a>

    <a href="{{ path('corps_crud_edit', {'id': corps.id}) }}">edit</a>

    {{ include('corps_crud/_delete_form.html.twig') }}
{% endblock %}
