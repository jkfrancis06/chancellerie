{% extends 'base.html.twig' %}

{% block title %}Corps index{% endblock %}

{% block body %}
    <h1>Corps index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Intitule</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for corps in corps %}
            <tr>
                <td>{{ corps.id }}</td>
                <td>{{ corps.intitule }}</td>
                <td>{{ corps.description }}</td>
                <td>
                    <a href="{{ path('corps_crud_show', {'id': corps.id}) }}">show</a>
                    <a href="{{ path('corps_crud_edit', {'id': corps.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('corps_crud_new') }}">Create new</a>
{% endblock %}
